import React from 'react';
import Dropdown from 'react-dropdown';


function About(){
    const options = [
        'one', 'two', 'three'
      ];
    return (
        <div>
        </div>
    );
}

export default About;